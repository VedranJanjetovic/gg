# Git for Windows defaults to core.autocrlf=true, which checks files out with
# CRLF line endings. gofmt then reports every Go file as unformatted, so pin the
# working tree to LF on every platform. Binary files are still auto-detected and
# left untouched by text=auto.
* text=auto eol=lf
